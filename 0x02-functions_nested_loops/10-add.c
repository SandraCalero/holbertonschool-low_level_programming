#include "holberton.h"
/**
 * add - adds two integers and returns the result
 * @num1: first number to add
 * @num2: second number to add
 *
 * Return: The result of an adds
 */
int add(int num1, int num2)
{
int result;
result = num1 + num2;
return (result);
}
